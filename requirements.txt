# =============================================
# Performance Lab API - requirements.txt (LEGACY)
# =============================================
#
# Prefer the new pyproject.toml + pip install -e ".[dev]" workflow.
# This file is kept for backward compatibility during transition.
#
# To install with modern tooling:
#   pip install -e ".[dev]"
#   # or for optional features:
#   pip install -e ".[dev,llm]"
# =============================================


# Core API stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Config & validation
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Database & migrations (Postgres, async-ready)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
alembic>=1.13.0
greenlet>=3.0.0

# Auth & security
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.1.0
bcrypt>=4.0.0

# HTTP client
httpx>=0.27.0

# JSON & performance
orjson>=3.10.0

# --- v0.3 Refined Math Model (Dose Engine + State Dynamics) ---
numpy>=1.26.0
scipy>=1.12.0

# --- LLM / AI (planned for prescriber layer) ---
anthropic>=0.30.0
openai>=1.35.0
google-generativeai>=0.7.0
mistralai>=0.4.0
tiktoken>=0.7.0
tenacity>=8.4.0

# --- Background tasks & rate limiting (planned) ---
celery[redis]>=5.4.0
redis>=5.0.0
slowapi>=0.1.0

# --- Logging ---
structlog>=24.4.0

# =============================================
# Development / Testing / Tooling
# =============================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

pyright>=1.1.0
ruff>=0.5.0
black>=24.0.0
isort>=5.13.0

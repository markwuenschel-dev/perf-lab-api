# =============================================
# Performance Lab API - requirements.txt (LEGACY)
# =============================================
#
# Prefer the new pyproject.toml + pip install -e ".[dev]" workflow.
# This file is kept for backward compatibility during transition.
#
# To install with modern tooling:
#   pip install -e ".[dev]"
#   # or for optional features:
#   pip install -e ".[dev,llm]"
#
# This file is synced to pyproject.toml's actual dependency groups (2026-07-17) — it
# had drifted in both directions: missing test deps needed to run the suite, while
# listing the optional llm/tasks/observability groups unconditionally instead of
# marking them optional as pyproject.toml does. Deleting this file outright is tracked
# separately in docs/REDESIGN_ROADMAP.md, bundled with removing the legacy root
# main.py once its /compute-metrics route is confirmed migrated — out of scope here.
# =============================================


# Core API stack
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Config & validation
pydantic-settings>=2.5.0
python-dotenv>=1.0.0

# Database & migrations (Postgres, async-ready)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
greenlet>=3.0.0

# Auth & security
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.1.0
bcrypt>=4.0.0

# HTTP client
httpx>=0.27.0

# JSON & file I/O
orjson>=3.10.0
aiofiles>=23.0.0

# --- v0.3 Refined Math Model (Dose Engine + State Dynamics) ---
numpy>=1.26.0
scipy>=1.12.0

# --- Data / Kaggle dataset access (KGAT token via KAGGLE_API_TOKEN) ---
kagglehub>=1.0.0

# =============================================
# Development / Testing / Tooling (pyproject.toml's [dev] extra)
# =============================================
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-xdist>=3.6.0
hypothesis>=6.100.0
pyright>=1.1.0
ruff>=0.5.0
black>=24.0.0
isort>=5.13.0
psycopg2-binary>=2.9.0
# Offline ML: Q2 recovery-priors training pipeline (app/ml/) + its tests. Not a
# runtime dependency — the app and shadow service consume only the JSON artifact.
pandas>=2.0.0
scikit-learn>=1.4.0

# =============================================
# Optional feature groups — NOT installed by default (matches pyproject.toml's
# [llm] / [tasks] / [observability] optional-dependency groups). Uncomment the
# section(s) you need, or use `pip install -e ".[dev,llm]"` etc. instead.
# =============================================

# --- [llm]: LLM / AI (planned for prescriber layer) ---
# anthropic>=0.30.0
# openai>=1.35.0
# google-generativeai>=0.7.0
# mistralai>=0.4.0
# tiktoken>=0.7.0
# tenacity>=8.4.0

# --- [tasks]: Background tasks & rate limiting (planned) ---
# celery[redis]>=5.4.0
# redis>=5.0.0
# slowapi>=0.1.0

# --- [observability]: Structured logging ---
# structlog>=24.4.0
